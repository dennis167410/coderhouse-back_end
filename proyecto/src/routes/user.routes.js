const {Router} = require('express');


const router = Router();

router.get('/', async(req, res) => {

})

router.post('/:userId/carts/:cartId', async(req, res) => {
     
})

router.delete('/:userId', async(req, res) => {
     
})

module.exports = router;